export const langPack = (object, language) => {
  if (language === "en") return object.en;
  if (language === "pt") return object.pt;
  if (language === "fr") return object.fr;
  if (language === "de") return object.de;
};
